# 分享、独立测试与自建平台

公开仓库与完整下载地址：<https://github.com/vh7t7gynrg-jpg/relationship-diagnosis>

这套工具的目标是统一题面、隔离来源和保留证据边界，不是生成“谁更坏”的自动分数。可按使用门槛选择以下方式。

## 最省事：直接分享 Skill

把完整 skill 目录发给使用者；若对方的 AI 不支持 Skills，则使用网页生成的“完整评估文本”，或复制 [standalone-prompt.md](standalone-prompt.md) 的完整代码块并在末尾附上答卷 JSON。便携文本已经包含核心判断规则和量化公式，不要求 AI 读取本目录中的其他文件。开始时只需说明模式：

- `S`：一人填写，适合快速判断；输出必须注明单一来源限制。
- `D`：两人用同一时间窗和题面独立填写；推荐用于完整关系评估。
- `M`：三人以上独立填写；按关系边分析，不以多数票定责。

所有参与者应在提交前不看他人答案，并使用代号而非真实姓名。

## 推荐：使用网页答卷平台

平台源码位于 [tester-site](tester-site/README.md)。标准流程：

1. 汇总者选择 S、D 或 M，生成一个会话编号，并把“邀请说明”发给参与者。
2. 每人使用相同会话编号、相同时间窗和各自唯一代号，在自己的设备上独立填写。
3. 每人导出 JSON；未完成填写的人不得先看他人文件。
4. 汇总者在“合并评测”中一次导入所有文件，先处理编号冲突、重复来源或安全警示。
5. 点击“复制完整评估文本”，粘贴到任意支持长文本的 AI 新对话中；不需要安装本 skill，也不要删除提示词前半部分。

平台不自动上传、保存或发送答卷；刷新页面会清空尚未导出的内容。JSON 文件本身可能含敏感信息，应由参与者通过其信任的渠道传递并在评测后妥善处理。

## 问卷深度

- 快速版：20个行为锚定题/人，约6–8分钟；适合先定位问题。
- 完整版：40题/人，约12–18分钟；适合主责、复合和长期去留。
- 背景模块：快速10题或完整24题，覆盖个人负荷、经济结构、家庭边界、照护、文化和未来计划；全部可跳过且不直接定责。
- 镜像行为题：每道题下同时填写本人自评与对他人的观察，使双方评价使用同一个行为定义；并排呈现不改变两类答案各自的来源属性。
- 感情与去留模块：分别收集本人感情倾向、可观察投入信号、继续意愿和共同调整能力；不合成“真爱概率”，不由一方替另一方填写内心。
- 两版都包含模式频率、趋势、影响、7项事件归属快照和分段代表事件；快速版答案可无损切换到完整版继续填写。

## 远程参与

`localhost` 只代表当前电脑，不能作为发给他人的访问地址。远程测试需要先把 `tester-site` 发布到可访问的网址，或由每位参与者在各自电脑上运行同一版本。发布后仍按以下方式保护独立性：

1. 发给所有参与者同一个平台网址、会话编号、证据模式和时间窗，不发送任何人的答案。
2. 每人使用唯一代号独立填写；提交前不查看、讨论或校对他人答案。
3. 平台仅在本机浏览器生成 JSON；参与者自行把文件交给指定汇总者。
4. 汇总者一次导入全部 JSON，先核验会话编号、重复参与者和安全警示，再生成 AI 评测提示。

## 兼容交换格式

- 当前版本：`relationship-evidence-v2`；平台继续读取 `relationship-evidence-v1`
- 文本编码：UTF-8 JSON
- 必需识别字段：`schemaVersion`、`sessionCode`、`mode`、`participant`、`counterparts`
- 没有相关场景或观察机会写 `N`，不确定写 `U`；两者和空白都不是零分。v2旧答卷没有 `N` 仍可兼容导入。
- v1按原10题计算覆盖率；v2按快速20题或完整40题计算，不得用新分母贬低旧答卷。
- 不要把不同会话编号的文件静默合并，也不要让同一参与者以多个文件重复计权。

## 隐私与安全

- 使用 A/B、P1/P2 等代号；删除姓名、账号、地址、单位、联系方式和私密影像。
- 若设备可能被监控、拒绝展示答案会引发报复，或存在暴力、强迫、跟踪、勒索等情况，不应开展联合对质。
- 平台和 skill 都不是紧急服务、法律认定、医学诊断或经过验证的心理量表。
- 记录增强只接受上下文足够且已脱敏的材料；截取片段不能自动压倒当事人的完整陈述。
