# Adaptive Interview Guide

这不是固定问卷。默认把最可能改变判断的缺口合并为一个精简信息包，让用户一次回答；用户已提供的信息不重复询问。

## Internal state

在内部维护，不必逐轮展示：

```text
goal: incident | pattern | health | breakup | repair | safety | compatibility
evidence_mode: S_single | D_dual | M_multi
records: none | present
status: together | uncertain | separated | unknown
source_mode: one_partner | both_partners | records | mixed
safety: unknown | no_signal | concern | urgent
incident_chain: {trigger, A_action, B_action, escalation, ending, repair}
pattern: {frequency, duration, severity, impact, exceptions}
agreements: []
repair_history: []
constraints: []
bottom_lines: []
evidence_for: []
evidence_against: []
unknowns: []
```

## First-turn routing

### User already gave a detailed event

先中性压缩其叙述，再一次性询问时间线、频率、修复和反证里仍关键的空缺。不要回到“请选择模式”。

从以下高价值缺口中选不超过 6 项：

1. 可观察的触发事实；
2. 双方第一句/第一个动作；
3. 明显升级点；
4. 是否有既有约定；
5. 24–72 小时内是否修复。

### User asks a broad question

用一个简短选项句确定范围，例如：

> 你现在最想判断的是最近一次冲突、长期相处模式，还是这段关系是否值得继续？

若目标是整段关系、长期责任或去留，下一条信息包必须包含模式选择：单视角 S、两人独立作答 D、三人以上独立作答 M，以及是否用 R 记录增强。详细执行读取 [perspective-protocol.md](perspective-protocol.md)。

### User asks for blame or percentages

先承认其需要一个清楚结论。普通聊天可给一次性精简信息包；用户要求正式测试或全面判断时，直接提供 [objective-items.md](objective-items.md) 的20题快速版，结论仍不稳定时扩展到40题，并同时收集7项归属快照和分段事件。不要先就百分比争论：

> 可以判断行为贡献，但百分比不是可靠测量。请在一条消息中描述一个代表冲突、是否重复、事后如何修复，以及一条可能不支持你当前看法的事实；不确定的可跳过。

### User mentions danger or coercion

停止普通批量访谈，读取 [safety.md](safety.md)。此时改用单题，第一问优先是当前安全或隐私，而不是追问全部经过。

## Default two-pass protocol

### Pass 1 — Consolidated intake

普通咨询根据目标选择 5–8 个编号项，要求用户在一条消息中回答。正式测试改用一次性20/40题表单，不受5–8项限制，但不得拆成多轮逐题询问。推荐顺序：

1. 证据模式 S / D / M、谁会作答，以及是否有 R 记录；
2. 当前状态和最想得到的决策；
3. 必要的安全筛查；
4. 一个代表事件的触发、双方行为、升级与结局；
5. 镜像核心领域中的相关项、频率、持续时间和影响；
6. 既有约定、修复和实际改变；
7. 双方最低条件/现实限制；
8. 一条反证或对方可能同意的事实。

已知项必须删除。题目应能用短句回答，不要求用户写长篇作文。

### Pass 2 — One optional follow-up batch

只有某些缺口会实质改变结论时，再合并询问 1–4 个补充项。第二批回答后必须输出评估；若用户未答，也按现有证据给阶段性判断。

首轮已经足够时跳过 Pass 2。输出后可以让用户选择是否继续细化，但不把继续追问作为获得结论的前提。

## Adaptive paths

### Quick path

适合用户只想快速判断、情绪负荷高或要求立即总结。通常在一个批次收集：

- 当前安全无紧迫风险；
- 一个具体事件；
- 该行为是偶发还是重复；
- 事后是否承认和修复；
- 一个可能改变判断的反证。

满足后就给阶段性评估。不要为了达到固定轮数继续问。

若用户所说“快速”是指正式量化测试，使用20题行为锚定快速版，而不是把正式测试缩减成数个宽泛印象题。

### Standard path

在 Quick path 基础上，补：

- 第二个不同时间或主题的事件，用于判断模式能否复制；
- 双方明确说过的需求与边界；
- 已尝试的改变及维持时间；
- 外部约束和最低目标是否可同时满足。

### Deep path

只有当用户愿意且细节确实能改善决策时进入。读取 [subjective-items.md](subjective-items.md)，把相关模块合并成一个有上限的批次，不要求全部完成。

### Two-partner path

双方都作答时：

- 先让双方独立描述同一事件，避免后一方只回应前一方的框架。
- 对双方使用同样的问题和时间窗口。
- 先比较一致事实，再列分歧；分歧本身不是撒谎证据。
- 不把表达更流畅、记忆更细或更会使用心理学词汇的一方视为更可信。
- 若存在恐惧、强迫或报复风险，不进行联合对质式访谈。
- 不能只让 A 转述 B 的答案。优先让双方在看不到对方回答时完成完全相同的空白表，再按 [perspective-protocol.md](perspective-protocol.md) 做四来源交叉比较。

## Choosing intake items

普通对话对候选问题做内部优先级判断，只保留最高价值的 5–8 项；正式量化测试使用固定20/40题镜像题库：

1. **安全性**：答案是否会改变即时安全处理？
2. **判别力**：能否区分两个合理解释？
3. **可回答性**：用户是否能用具体事实回答？
4. **负担**：是否能用更短、更少侵入的问题得到同样信息？

优先询问“行为 + 顺序 + 结果”，少问抽象的“为什么”。动机只能在明确是本人陈述时记录为当事人解释。避免把一个自然问题人为拆成多个回合。

## Periodic summaries

首轮或补充轮结束后，简短总结：

- 目前较确定的 1–3 点；
- 仍有两个解释的 1 点；
- 是否继续深入或先给阶段性评估。

默认先总结，不再询问用户是否“允许总结”。总结后可提供继续深入的选项。

## Counterevidence gate

在形成明显偏向某一方的结论前，至少完成一种反证检查：

- “哪条事实最不支持‘主要是对方的问题’？”
- “如果只看这次事件，你自己最希望重做哪个行为？”
- “对方会如何描述同一时刻？其中哪些有可观察事实支持？”
- 检查是否存在未提及的既有约定、前序挑衅或合理暂停说明。

反证用于校准，而不是强迫用户自责。高危行为不要求受害者寻找自己的“对等责任”。

## Stop and synthesis rules

在以下任一条件满足时停止追问并总结：

- 已足够回答用户当前的窄问题；
- 新问题只会增加细节，不会改变建议；
- 用户要求暂停、跳过或直接总结；
- 用户情绪明显过载；
- 关键事实不可获得，继续问也无法提高可信度；
- 安全问题要求转向现实支持。

只有以下缺口会实质改变结论时才追问：当前危险、行为归属、是否重复、是否有明确同意/约定、是否有报复或控制、双方最低目标。

## Synthesis readiness

“阶段性判断”可随时给出。“较完整评估”通常至少需要：

- 一个可还原的具体事件；
- 模式的频率/持续时间和影响；
- 修复或改变记录；
- 必要的安全信息；
- 一个反证或竞争解释；
- 若讨论去留，双方最低目标与现实约束。

没有双方面谈或独立记录时，不声称查明了全部事实；可以对已描述行为作出明确评价。
